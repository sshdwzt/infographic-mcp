[project]
name = "design-inspiration-mcp"
version = "1.0.0"
description = "An MCP server that fetches design inspiration using the Serper API"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.26.0",
]
